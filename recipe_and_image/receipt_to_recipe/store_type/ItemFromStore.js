const store = require('./FindStore');

async function getIngredients () {
  storeName = store();
  ingredients;
  switch (storeName) {
    case 0:

    case 1:

    case 2:

    case 3:

  }
  return ingredients;
}

module.exports = getIngredients;
